docling>=2.0
numpy>=1.24
Pillow>=10.0
# Для русского OCR без установки Tesseract:
# pip install easyocr
# Для захвата страницы в браузере (scroll_long_screenshot.py capture):
# pip install playwright && playwright install chromium
